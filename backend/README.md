# Document Management System - Backend

This is the backend for the Document Management System, built with Spring Boot.

## Configuration

This application requires the following environment variable to be set:

- `APP_JWT_SECRET`: A secret key for signing JWTs. It should be a long, random string.

Example:
`export APP_JWT_SECRET=your-super-secret-key-that-is-long-and-random`
